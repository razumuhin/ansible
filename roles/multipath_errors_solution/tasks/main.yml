- name: multipath.conf dosyasına blacklist block ekle
  blockinfile:
    path: /etc/multipath.conf
    block: |
      blacklist {
        devnode "^(ram|raw|loop|fd|md|dm-|sr|scd|st|sda)[0-9]*"
      }
